// import admin from 'firebase-admin'
// import { getApp } from 'firebase-admin/app';

// var serviceAccount = require(process.env.NEXT_PUBLIC_GOOGLE_APPLICATION_CREDENTIALS);

// let firebase_admin;

// firebase_admin = getApp().length === 0 ? admin.initializeApp({credential: admin.credential.cert(serviceAccount)}) : getApp()[0];

// export default firebase_admin;